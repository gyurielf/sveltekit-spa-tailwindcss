// import Cookies from 'js-cookie';

export const isLoggedIn = () => {
	// return (Cookies.get('authToken') as string) !== undefined || false;
	// Return fake true for testing purposes
	return true;
};
